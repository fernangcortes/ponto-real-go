{
  "name": "ponto-real-go-web",
  "version": "1.1.2",
  "private": true,
  "description": "Ferramentas de lint do front-end. O aplicativo NÃO tem build: web/ é servido exatamente como está, embutido no binário Go via go:embed. O Node é necessário só para rodar o lint.",
  "scripts": {
    "lint": "eslint web/",
    "test": "node --test \"web/js/*.test.js\""
  },
  "devDependencies": {
    "eslint": "^9.39.0"
  }
}
